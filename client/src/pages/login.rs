use yew::prelude::*;

#[function_component(Login)]
pub fn login() -> Html {
    html! {
        <div class="border rounded-sm flex items-center justift-center h-screen filter drop-shadow-lg">
            { "Login page" }
        </div>
    }
}
